import React from 'react';
// import ScheduleCard from './ScheduleCard';

export default function Shedule(props) {
  // const { schedule } = props.userData;
  // console.log(schedule);
  return (
    <div>
      <p>teste</p>
    </div>
  )
};
